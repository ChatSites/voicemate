
// Re-export from the hooks directory to maintain compatibility with imports
import { useToast, toast, ToastProvider, ToasterToast } from "@/hooks/use-toast";

export { useToast, toast, ToastProvider, ToasterToast };
